{
  "name": "civicos",
  "version": "1.0.0",
  "type": "module",
  "description": "CivicOS: Sovereign AI Government Benefits Discovery Platform",
  "scripts": {
    "start": "./start.sh",
    "start:docker": "./start.sh --docker",
    "dev": "cd frontend && npm run dev",
    "dev:backend": "PORT=8090 cd backend && npm run start",
    "dev:frontend": "BACKEND_PORT=8090 cd frontend && npm run dev",
    "build:frontend": "cd frontend && npm run build",
    "test": "cd backend && npm test",
    "test:evaluate": "curl -s -X POST http://localhost:8090/api/evaluate -H 'Content-Type: application/json' -d '{\"query\":\"I am a 21-year-old student from Uttar Pradesh\"}'",
    "compose:up": "cd infra && docker compose up -d",
    "compose:down": "cd infra && docker compose down"
  },
  "keywords": ["civicos", "sovereign-ai", "opensearch", "corretto", "firecracker", "cedar", "strands"],
  "author": "DeepMind Civic Systems",
  "license": "Apache-2.0"
}
